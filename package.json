{
  "name": "play-gulp-standalone",
  "version": "1.0.0",
  "dependencies": {
    "gulp": "~3.9.0",
    "bower": "~1.7.7",
    "jspm": "~0.16.27"
  },
  "devDependencies": {
    "yo":  "~1.6.0"
  },
  "engines": {
    "node": "5.5.0"
  },
  "scripts": {
    "postinstall": "cd ui && jspm config registries.github.timeouts.lookup 120 && jspm install && npm install && gulp build"
  }
}
